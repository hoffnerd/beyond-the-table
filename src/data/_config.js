
/*
    Each component may have their own config objects. 
    This is just a master one of either shared settings acrossed multiple componets, 
    or just easier to have here so I dont have to search multiple files to turn off random features.
*/

/* example - bool, this is just an example */
export const example = true;

/* animationsActive - bool, are the card animations active or not */
export const animationsActive = true;

/* amountOfCardsToLoad - int, number of cards to load for the CharacterCards component */
export const amountOfCardsToLoad = 12;
